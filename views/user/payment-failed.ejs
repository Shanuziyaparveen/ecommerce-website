<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: #f9f9f9;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-align: center;
            padding: 20px;
            box-sizing: border-box;
        }

        .container {
            background: #fff;
            padding: 40px 30px;
            border-radius: 15px;
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }

        h1 {
            font-size: 2.5rem;
            color: #d32f2f; /* Red */
            margin-bottom: 15px;
        }

        p {
            font-size: 1.1rem;
            line-height: 1.6;
            color: #666;
            margin-bottom: 20px;
        }

        .error-message {
            font-size: 1.2rem;
            color: #f44336; /* Red */
            font-weight: bold;
            margin: 15px 0;
            padding: 10px;
            border: 2px solid #f44336;
            border-radius: 5px;
        }

        .warning-icon {
            font-size: 4rem;
            color: #ff9800; /* Orange */
            margin-bottom: 15px;
        }

        .actions {
            margin-top: 25px;
        }

        .actions a {
            display: inline-block;
            margin: 0 15px;
            padding: 12px 25px;
            text-decoration: none;
            font-size: 1rem;
            border-radius: 5px;
            transition: all 0.3s ease;
        }

        .actions .cart-link {
            background: #d32f2f; /* Red */
            color: #fff;
        }

        .actions .cart-link:hover {
            background: #b71c1c; /* Darker Red */
        }

        .actions .retry-link {
            background: #ff9800; /* Orange */
            color: #fff;
        }

        .actions .retry-link:hover {
            background: #f57c00; /* Darker Orange */
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 2rem;
            }

            p {
                font-size: 1rem;
            }

            .actions a {
                font-size: 0.9rem;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="warning-icon">
            ⚠️
        </div>
        <h1>Payment Failed</h1>
        <p>We couldn't process your payment:</p>
        <p class="error-message"><%= errorMessage %></p>
        <div class="actions">
            <a href="/cart" class="cart-link">Go back to Cart</a>
            <a href="/checkout" class="retry-link">Try Again</a>
        </div>
    </div>
</body>
</html>
